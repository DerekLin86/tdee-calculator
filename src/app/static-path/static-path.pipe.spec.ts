import { StaticPathPipe } from './static-path.pipe';

describe('StaticPathPipe', () => {
  it('create an instance', () => {
    const pipe = new StaticPathPipe();
    expect(pipe).toBeTruthy();
  });
});
